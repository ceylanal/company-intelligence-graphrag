# Browser requests are always same-origin through /api. Do not define a
# NEXT_PUBLIC API URL, provider key, service-account credential, or token.
# The following values are server-only Vercel Function configuration. BACKEND_API_KEY
# is a Vercel Sensitive variable; it must never be a NEXT_PUBLIC_* value.
CLOUD_RUN_STAGING_URL=https://company-graphrag-staging-77096651349.europe-west1.run.app
GCP_PROJECT_NUMBER=77096651349
GCP_WORKLOAD_IDENTITY_POOL_ID=vercel-staging
GCP_WORKLOAD_IDENTITY_POOL_PROVIDER_ID=vercel-staging
GCP_SERVICE_ACCOUNT_EMAIL=graphrag-vercel-stg-invoker@PROJECT_ID.iam.gserviceaccount.com
PROXY_CONNECT_TIMEOUT_MS=15000
BACKEND_API_KEY=
