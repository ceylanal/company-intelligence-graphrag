# Browser requests are always same-origin through /api. Do not define a
# NEXT_PUBLIC API URL, provider key, service-account credential, or token.
# The following values are server-only Vercel Function configuration; their values
# are identifiers/URLs, never credential material.
CLOUD_RUN_STAGING_URL=https://company-graphrag-staging-c6oeawtcxq-ew.a.run.app
GCP_PROJECT_NUMBER=
GCP_WORKLOAD_IDENTITY_POOL_ID=vercel-staging
GCP_WORKLOAD_IDENTITY_POOL_PROVIDER_ID=vercel-staging
GCP_SERVICE_ACCOUNT_EMAIL=company-graphrag-vercel-staging-invoker@PROJECT_ID.iam.gserviceaccount.com
PROXY_CONNECT_TIMEOUT_MS=15000
