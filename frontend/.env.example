# Public URL only. Never put API keys or provider credentials in NEXT_PUBLIC_*.
# Staging points to the existing Cloud Run service; local development uses localhost.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
